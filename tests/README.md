# Tests

This repo uses lightweight Node scripts instead of a test framework.

Run:

```sh
npm test
```

Coverage:

- schema and fixture shape checks
- Markdown link checks
- private leak and public-risk phrase checks

The scripts live in [../scripts/](../scripts/).
